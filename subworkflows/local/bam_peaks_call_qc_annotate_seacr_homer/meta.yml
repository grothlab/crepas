# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/subworkflows/yaml-schema.json
name: "bam_peaks_call_qc_annotate_seacr_homer"
description: |
  Call peaks from bedGraph signal with SEACR, compute FRiP scores, and (optionally) annotate
  peaks with HOMER and generate peak-annotation QC plots
keywords:
  - seacr
  - peak-calling
  - cut-and-run
  - frip
  - homer
  - annotation
components:
  - seacr/callpeak
  - homer/annotatepeaks
  - frip_score
  - multiqc_custom_peaks
  - plot_homer_annotatepeaks
input:
  - ch_bedgraph_control:
      description: |
        IP bedGraph file(s) and matched input control bedGraph file(s), when a control exists
        Structure: [ val(meta), [ path(ip_bedgraph) ], [ path(ipcontrol_bedgraph) ] ]
  - seacr_peak_threshold:
      type: float
      description: SEACR peak calling threshold (numeric threshold, used when no input control is available)
  - ch_bam:
      description: |
        IP BAM file(s) and matched input control BAM file(s), used for FRiP scoring
        Structure: [ val(meta), path(ip_bam), path(control_bam) ]
  - ch_fasta:
      type: file
      description: |
        Reference fasta file
        Structure: [ val(meta), path(fasta) ]
  - ch_gtf:
      type: file
      description: |
        Reference GTF annotation file
        Structure: [ val(meta), path(gtf) ]
  - annotate_peaks_suffix:
      type: string
      description: Suffix trimmed off input HOMER annotatePeaks filenames when grouping for QC plots
  - ch_peak_count_header_multiqc:
      type: file
      description: MultiQC custom-content header for the peak count table
  - ch_frip_score_multiqc:
      type: file
      description: MultiQC custom-content header for the FRiP score table
  - ch_peak_annotation_header_multiqc:
      type: file
      description: MultiQC custom-content header for the peak annotation plot
  - skip_peak_annotation:
      type: boolean
      description: Skip HOMER peak annotation (and downstream peak-annotation QC)
      pattern: "true|false"
  - skip_peak_qc:
      type: boolean
      description: Skip peak-annotation QC plots (only used when skip_peak_annotation is false)
      pattern: "true|false"
output:
  - peaks:
      description: |
        SEACR peaks, filtered to remove samples with zero peaks called
        Structure: [ val(meta), path(peaks) ]
  - frip_txt:
      description: |
        FRiP score text file
        Structure: [ val(meta), path(txt) ]
  - frip_multiqc:
      description: |
        FRiP score MultiQC custom-content file
        Structure: [ val(meta), path(tsv) ]
  - peak_count_multiqc:
      description: |
        Peak count MultiQC custom-content file
        Structure: [ val(meta), path(tsv) ]
  - homer_annotatepeaks:
      description: |
        HOMER peak annotation output, when skip_peak_annotation is false
        Structure: [ val(meta), path(txt) ]
  - plot_homer_annotatepeaks_txt:
      description: |
        Peak annotation QC plot data, when skip_peak_annotation and skip_peak_qc are false
        Structure: [ val(meta), path(txt) ]
  - plot_homer_annotatepeaks_pdf:
      description: |
        Peak annotation QC plot, when skip_peak_annotation and skip_peak_qc are false
        Structure: [ val(meta), path(pdf) ]
  - plot_homer_annotatepeaks_tsv:
      description: |
        Peak annotation QC plot MultiQC custom-content file, when skip_peak_annotation and
        skip_peak_qc are false
        Structure: [ val(meta), path(tsv) ]
authors:
  - "@samuelruizperez"
maintainers:
  - "@samuelruizperez"
